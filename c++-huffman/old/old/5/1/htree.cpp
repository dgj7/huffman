#include "htree.h"
/*
//==================
// HLeaf
//==================
HLeaf::HLeaf( char data, ulint freq )
// :_frequency( freq ), _data( data )
{
	_frequency = freq;
	_data = data;
}

//==================
// HList
//==================
void HList::insert( char input )
{
	// if the tree is empty, just put the item in the list
	if( _list.empty(  ) )
	{
		HLeaf temp( input, 1 );
		_list.push_back( temp );
	}
}


*/
